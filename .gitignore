node_modules/
dist/
build/
coverage/
.env
.env.local
*.log
.pgdata/
.DS_Store
frontend/src/fontes.css

# Ajuste de máquina/pessoa no Claude Code. O que é DA CASA — o hook de início de
# sessão e o settings.json — é versionado de propósito: é ele que faz a sessão
# nova subir com banco, dependências e Chromium prontos.
.claude/settings.local.json
