/**
 * PORTA PÚBLICA — módulo `archive`
 *
 * Fila de cópia documental para o Shared Drive institucional: enfileirar no
 * fechamento, processar com retentativa idempotente, reconciliar.
 */
export { ArchiveModule } from './archive.module';
export { ArchiveService } from './archive.service';
