/**
 * PORTA PÚBLICA — módulo `incidents`
 * Ocorrências especiais, contenção, síntese técnica, anexos protegidos e
 * comunicação externa (registrada e aprovada — nunca enviada).
 */
export { IncidentsModule } from './incidents.module';
export { IncidentsService, CATEGORIAS } from './incidents.service';
