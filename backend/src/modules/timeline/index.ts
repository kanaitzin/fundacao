/**
 * PORTA PÚBLICA — módulo `timeline`
 * Linha do tempo unificada e painel da casa. Agrega provedores registrados
 * no kernel; não conhece nenhum módulo de domínio.
 */
export { TimelineModule } from './timeline.module';
export { TimelineService } from './timeline.service';
export type { TimelineMode } from './timeline.service';
